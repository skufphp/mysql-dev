services:
  # База данных MySQL
  mysql-dev:
    image: mysql:8.4
    container_name: mysql-dev
    restart: unless-stopped
    environment:
      # Пароль root (берется из .env)
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    ports:
      # Проброс порта на хост-машину
      - "${MYSQL_PORT}:3306"
    volumes:
      # Хранилище данных
      - mysql-data-dev:/var/lib/mysql
    networks:
      - mysql-dev-network
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-p$$MYSQL_ROOT_PASSWORD" ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  # Интерфейс управления БД (phpMyAdmin)
  phpmyadmin-dev:
    image: phpmyadmin
    container_name: phpmyadmin-dev
    restart: unless-stopped
    environment:
      # Хост БД для подключения
      PMA_HOST: ${PMA_HOST}
    ports:
      # Порт для доступа к веб-интерфейсу phpMyAdmin
      - "${PHPMYADMIN_PORT}:80"
    networks:
      - mysql-dev-network
    healthcheck:
      test: [ "CMD", "wget", "-q", "-O", "-", "http://localhost/" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    depends_on:
      mysql-dev:
        condition: service_healthy

volumes:
  mysql-data-dev:

networks:
  mysql-dev-network:
    name: mysql-dev-network